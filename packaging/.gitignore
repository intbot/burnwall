# Artifacts produced by the release workflow (and local test runs) — never committed.
# The native binaries are downloaded from the GitHub Release at publish time.
nuget/native/
nuget/bin/
nuget/obj/
nuget-out/
pypi/burnwall_launcher/_bin/
pypi/_unpack/
pypi/stage/
pypi/build/
pypi/*.egg-info/
dist_wheels/
artifacts/
